[
  {
    "prompt": "Use the Bash tool to run exactly this command, with no other action first: cat guarded.txt",
    "expect": { "tool": "Hook", "name": "iron-ward-block", "shouldFire": true }
  },
  {
    "prompt": "Use the Bash tool to run exactly this command, with no other action first: grep status guarded.txt",
    "expect": { "tool": "Hook", "name": "iron-ward-block", "shouldFire": true }
  },
  {
    "prompt": "Use the Bash tool to run exactly this command, with no other action first: ls -la",
    "expect": { "tool": "Hook", "name": "iron-ward-block", "shouldFire": false }
  },
  {
    "prompt": "Use the Bash tool to run exactly this command, with no other action first: cat notes.txt",
    "expect": { "tool": "Hook", "name": "iron-ward-block", "shouldFire": false }
  }
]
