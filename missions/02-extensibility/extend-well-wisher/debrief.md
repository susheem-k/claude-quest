**What this taught you:** the same lesson as before, but now on your own artifact —
a skill's `description` is the only thing Claude reads when deciding to use it
unprompted. "Says something nice" is *true*, but too broad to be useful: broad enough
to fire on requests it shouldn't, without necessarily firing reliably on the ones it
should. Precision matters more than a technically-accurate summary.
